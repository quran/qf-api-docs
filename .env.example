# Legend:
# - Required (prod): must be set in production when the feature is enabled
# - Optional: has a default value (noted)
# - Dev-only: used for local/dev convenience
# Safe to expose; no secrets here.

# Optional (dev-only). Default: https://qf-form-handler.fly.dev
# Safe to expose; no secrets here.
SCOPE_REQUEST_API_BASE_URL=https://qf-form-handler.fly.dev
